# [게임 기획서] 탑 쌓기 게임 (SkyStack)

## 1. 게임 개요 (Overview)
* **게임 제목**: 스카이 스택 (Sky Stack)
* **장르**: 원터치 캐주얼 타이밍 액션
* **목표**: 좌우로 움직이는 블록을 타이밍에 맞춰 정지시켜 최대한 높은 층을 쌓아 최고 기록을 세운다
* **플랫폼**: 모바일 웹 브라우저 및 PC 웹 브라우저 (HTML5 Single-file)
* **화면 표시**: 게임은 고정된 논리 화면 비율을 유지한다. 전체화면과 창 모드에서는 같은 비율로 확대 또는 축소하며, 화면 모드 변경은 게임 규칙과 블록·글자의 크기 비율에 영향을 주지 않는다. 게임 영역 밖의 여백은 검은색으로 표시하고, 떨어지는 블록 조각을 포함한 모든 게임 요소는 게임 화면 바깥에 보이지 않도록 제한한다.

## 2. 핵심 조작 (controls)
* **PC 조작**: 키보드 스페이스바, 마우스 좌클릭
* **모바일 조작**: 화면 어디든 터치
* **입력 흐름**: 시작 화면의 첫 입력은 게임 시작에만 사용한다. 플레이 중 입력은 블록을 멈추는 데 사용하며, 게임오버 상태의 입력은 즉시 새 게임을 시작한다.

## 3. 핵심 규칙 및 게임 루프 (Core Mechanics & Loop)
1. **블록 이동 (Movement)**
   * 블록은 화면 좌우를 자동으로 왕복한다.
   * 플레이어가 입력하기 전까지 계속 움직이며, 화면 경계에 닿으면 즉시 반대 방향으로 진행한다.
   * 새 블록은 화면의 왼쪽 또는 오른쪽에서 무작위로 등장하며, 등장한 쪽의 반대 방향으로 이동한다.
   * 게임 시작 시에는 낮은 속도로 이동하고, 층수가 높아질수록 이동 속도가 점진적으로 증가한다. 단, 게임 난도가 과도하게 상승하지 않도록 최대 이동 속도를 제한한다.
   * 새 블록은 직전 층에 남은 블록의 너비를 이어받는다.
2. **정지 및 절단 (stop & slice)**
   * 플레이어가 입력하면 움직이던 블록은 즉시 그 자리에서 멈춘다.
   * 새 블록과 바로 아래 블록이 겹치는 가로 영역만 다음 층으로 남는다.
   * 아래 블록의 범위를 벗어난 부분은 잘려 나가며, 원래 진행 방향을 따라 바닥으로 떨어진다. 바닥에 닿으면 짧게 튀어 오른 뒤 사라진다.
   * 남은 블록의 가로폭은 다음에 등장하는 블록의 가로폭이 된다. 따라서 조금씩 어긋날수록 탑을 쌓기 어려워진다.
   * 두 블록이 전혀 겹치지 않으면 남는 영역 없이 즉시 게임오버가 된다.
   
3. **퍼펙트 판정 및 콤보 (Combo)**
   * 새 블록의 중심이 바로 아래 블록의 중심과 거의 일치하면 **퍼펙트**로 판정한다.
   * 퍼펙트가 발생하면 블록 중심을 아래 블록 중심에 정확히 정렬하고, 블록을 절단하지 않은 채 현재 블록 기준 두 단계 아래 블록의 너비로 복구한다. 두 단계 아래 블록이 없으면 바닥 블록의 너비를 사용한다.
   * 퍼펙트 성공 시 콤보가 1씩 증가하며, 일반적으로 블록을 쌓으면 콤보는 0으로 초기화된다.
   * 콤보가 이어질수록 퍼펙트 텍스트, 색상 변화, 짧은 화면 흔들림 등 보상 연출을 강화한다.
   * 콤보는 별도의 점수 배율보다 정확한 플레이를 칭찬하는 시각·청각 보상으로 사용한다. 바닥 블록은 점수에 포함하지 않으며, 성공적으로 쌓은 블록 한 층당 기본 점수는 1점씩 증가한다.

4. **게임오버 조건 (Game Over)**
   * 멈춘 새 블록이 바로 아래 블록과 가로로 전혀 겹치지 않으면 게임오버가 된다.
   * 실패한 블록은 진행 방향을 유지한 채 아래로 떨어지고, 탑 쌓기 동작은 즉시 정지한다.
   * 화면에는 현재 점수와 최고 기록, 그리고 재시작 안내를 표시한다.
   * 현재 점수가 최고 기록보다 높으면 최고 기록을 갱신했다는 메시지를 함께 표시한다.
   * 플레이어가 스페이스바, 클릭 또는 화면 터치를 입력하면 같은 화면에서 즉시 새 게임을 시작한다.

## 4. 연출 및 시각/청각 피드백 (Juice Up)
* **비주얼 연출**
  * 층이 올라갈 때마다 블록의 색상이 파스텔 무지개 톤(HSL 색상환)으로 부드럽게 변화한다.
  * 블록을 성공적으로 놓을 때마다 카메라 시점이 부드럽게 이동.
  * 퍼펙트 성공 시 반짝이는 파티클 효과 및 화면 미세 진동(Screen Shake)
* **사운드 (Web Audio API)**
  * 착지음: 경쾌한 '퐁' 소리
  * 퍼펙트 연속 콤보음: '도-레-미-파-솔-라-시-도' 순서로 음정이 점점 올라가는 벨소리
  * 게임 오버음: 둔탁한 하강 저음

## 5. UI 및 데이터 저장 
* **플레이 중 UI**: 화면 상단에 현재 성공적으로 쌓은 층수와 역대 최고 층수를 함께 표시한다. 새 게임을 시작하면 현재 층수는 0으로 초기화하고, 역대 최고 층수는 유지한다.
* **시작 안내**: 게임 시작 전에는 조작 방법과 목표를 짧게 안내한다.
* **게임 오버 UI**: 최종 층수, 역대 최고 층수와 재시작 방법 ('다시하기 / RESTART')을 표시한다.
* **최고 기록 저장**: 브라우저 `LocalStorage`에 역대 최고 층수(`highScore`)를 저장하여 재접속 시에도 유지한다. 현재 층수가 저장된 최고 기록보다 높아질 때만 최고 기록을 갱신한다.
