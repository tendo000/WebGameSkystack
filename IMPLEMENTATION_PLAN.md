# 스카이 스택 프로토타입 구현계획서

## 1. 프로토타입 목표

`WEBGAMESKYSTACK.md`의 핵심 규칙을 동작하는 형태로 검증한다. 게임 규칙과 플레이 흐름의 기준 문서는 GDD이며, 이 문서는 그 규칙을 구현하는 기술 계획만 다룬다. 이번 단계에서는 연출, 효과음, 시작·결과 UI, 최고 기록 저장 기능을 구현하지 않는다.

## 2. 구현 범위

* `index.html` 한 파일에 HTML, CSS, 순수 JavaScript를 모두 작성한다.
* HTML5 Canvas 2D로 배경과 블록을 직접 그린다.
* GDD에 정의된 키보드 스페이스바, 마우스 좌클릭, 모바일 터치 입력을 하나의 입력 처리 함수로 연결한다.
* 브라우저 창 크기가 바뀌면 캔버스 크기와 게임 좌표를 다시 계산한다.

## 3. 상태 구현

| 상태 | 역할 | 입력 결과 |
| --- | --- | --- |
| `ready` | 초기 바닥 블록과 첫 이동 블록을 준비한다. | GDD의 시작 입력 흐름을 처리한다. |
| `playing` | 이동 블록을 매 프레임 갱신한다. | GDD의 적층 판정을 실행한다. |
| `gameover` | 블록이 전혀 겹치지 않아 게임을 멈춘 상태다. | GDD의 재시작 입력 흐름을 처리한다. |

## 4. 데이터 구조

* 블록은 가로 위치 `x`, 세로 위치 `y`, 가로폭 `width`, 높이 `height`, 이동 속도 `speed`, 이동 방향 `direction`을 가진다.
* `stack` 배열에는 바닥 블록과 성공적으로 적층된 블록만 저장한다.
* `movingBlock`은 현재 좌우로 움직이는 블록 한 개다.
* `score`와 `combo`는 GDD에 정의된 점수·콤보 규칙을 표시하고 판정하는 상태값이다.

## 5. 업데이트와 렌더링 흐름

1. `requestAnimationFrame` 반복문에서 이전 프레임 이후 시간을 계산한다.
2. `playing` 상태이면 `movingBlock`의 `x` 좌표를 GDD의 이동 규칙에 따라 갱신한다.
3. 이동 블록이 화면 좌우 경계에 닿으면 위치를 경계 안으로 보정하고 방향을 반전한다.
4. 매 프레임 배경, 적층 블록, 이동 블록 순서로 캔버스에 그린다.
5. 화면 크기가 변경되면 기준 논리 크기를 유지한 채 캔버스 표시 크기와 좌표 비율을 다시 계산한다.

## 6. 적층 판정 구현

1. 플레이어 입력 시 `movingBlock`과 `stack`의 마지막 블록의 가로 겹침 구간을 계산한다.
2. 계산한 겹침 폭과 중심 오차를 GDD의 게임오버·퍼펙트·일반 적층 규칙에 적용한다.
3. 확정된 블록을 `stack`에 추가하고, GDD 규칙에 따라 점수·콤보 상태를 갱신한다.
4. 다음 이동 블록에는 확정 블록의 폭을 전달하고, 이동 속도는 GDD의 난이도 곡선에 맞게 계산한다.

## 7. 프로토타입 완료 기준

* GDD의 블록 이동, 정지 및 절단, 퍼펙트 및 콤보, 게임오버 규칙이 모두 동작한다.
* GDD에 정의된 모든 입력 방식에서 같은 규칙으로 플레이할 수 있다.
* 창 크기를 바꿔도 블록이 캔버스 밖으로 벗어나지 않는다.
