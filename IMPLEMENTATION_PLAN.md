# 스카이 스택 프로토타입 구현계획서

## 1. 프로토타입 목표

`WEBGAMESKYSTACK.md`의 핵심 규칙을 동작하는 형태로 검증한다. 게임 규칙과 플레이 흐름의 기준 문서는 GDD이며, 이 문서는 그 규칙을 구현하는 기술 계획만 다룬다. 이번 단계에서는 연출과 효과음은 구현하지 않으며, 현재 층수·최고 층수 표시와 최고 기록 저장 기능은 구현한다.

## 2. 구현 범위

* `index.html` 한 파일에 HTML, CSS, 순수 JavaScript를 모두 작성한다.
* HTML5 Canvas 2D로 배경과 블록을 직접 그린다.
* GDD에 정의된 키보드 스페이스바, 마우스 좌클릭, 모바일 터치 입력을 하나의 입력 처리 함수로 연결한다.
* 게임은 고정 논리 해상도로 계산하고, 브라우저 창 크기가 바뀌면 캔버스 표시 배율과 여백만 다시 계산한다. 따라서 전체화면과 창 모드에서도 게임 규칙과 요소의 크기 비율이 유지된다.
* 게임 영역 밖의 여백은 검은색으로 그린다. 캔버스의 그리기 영역을 게임 화면 크기로 제한해 블록과 떨어지는 조각이 여백에 표시되지 않게 한다.

## 3. 상태 구현

| 상태 | 역할 | 입력 결과 |
| --- | --- | --- |
| `ready` | 초기 바닥 블록과 첫 이동 블록을 준비한다. | GDD의 시작 입력 흐름을 처리한다. |
| `playing` | 이동 블록을 매 프레임 갱신한다. | GDD의 적층 판정을 실행한다. |
| `gameover` | 블록이 전혀 겹치지 않아 게임을 멈춘 상태다. | GDD의 재시작 입력 흐름을 처리한다. |

## 4. 데이터 구조

* 블록은 가로 위치 `x`, 세로 위치 `y`, 가로폭 `width`, 높이 `height`, 이동 속도 `speed`, 이동 방향 `direction`을 가진다.
* `stack` 배열에는 바닥 블록과 성공적으로 적층된 블록만 저장한다.
* `movingBlock`은 현재 좌우로 움직이는 블록 한 개다.
* `score`와 `combo`는 GDD에 정의된 점수·콤보 규칙을 표시하고 판정하는 상태값이다.
* `highScore`는 브라우저 `LocalStorage`의 `highScore` 키에서 불러온 역대 최고 층수다. 새 게임을 시작해도 초기화하지 않으며, `score`가 더 커질 때만 갱신해 다시 저장한다.
* 다음 이동 블록의 `width`는 직전 확정 블록의 `width`를 사용한다. 단, 퍼펙트로 확정된 블록은 현재 블록 기준 두 단계 아래 블록의 너비로 복구한 뒤 저장한다. 두 단계 아래 블록이 없으면 바닥 블록의 너비를 사용한다.
* 새 이동 블록의 출발 쪽은 좌·우 중 무작위로 선택하며, `direction`은 출발 쪽의 반대 방향으로 설정한다.

## 5. 업데이트와 렌더링 흐름

1. `requestAnimationFrame` 반복문에서 이전 프레임 이후 시간을 계산한다.
2. 새 이동 블록을 만들 때 좌·우 중 하나를 무작위 출발 쪽으로 정하고, `movingBlock`의 `x` 좌표와 `direction`을 그 출발 쪽에 맞춰 설정한다.
3. `playing` 상태이면 `movingBlock`의 `x` 좌표를 GDD의 이동 규칙에 따라 갱신한다.
4. 이동 블록이 화면 좌우 경계에 닿으면 위치를 경계 안으로 보정하고 방향을 반전한다.
5. 매 프레임 배경, 적층 블록, 이동 블록 순서로 캔버스에 그린다.
6. 화면 크기가 변경되면 고정 논리 화면을 현재 화면에 맞춰 같은 비율로 확대 또는 축소하고, 남는 영역은 검은색 여백으로 표시한다.
7. 렌더링 전 게임 화면 영역으로 그리기 범위를 제한해 블록과 떨어지는 조각이 게임 화면 밖에 남지 않게 한다.
8. 매 프레임 현재 층수와 역대 최고 층수를 함께 표시한다. 게임오버 화면에는 최종 층수와 역대 최고 층수를 표시한다.

## 6. 적층 판정 구현

1. 플레이어 입력 시 `movingBlock`과 `stack`의 마지막 블록의 가로 겹침 구간을 계산한다.
2. 중심 오차가 퍼펙트 허용 범위 안이면 블록 중심을 아래 블록과 정렬하고, 블록 너비를 현재 블록 기준 두 단계 아래 블록의 너비로 복구한다. 두 단계 아래 블록이 없으면 바닥 블록의 너비를 사용한다.
3. 퍼펙트가 아니면 겹친 영역만 확정 블록으로 남긴다. 겹친 영역이 없으면 게임오버로 전환한다.
4. 확정된 블록을 `stack`에 추가하고, GDD 규칙에 따라 점수·콤보 상태를 갱신한다.
5. 다음 이동 블록은 직전 확정 블록의 너비를 이어받는다. 따라서 일반 적층에서는 줄어든 너비가 유지되고, 퍼펙트 직후에는 두 단계 아래 블록에서 복구한 너비가 적용된다.
6. 이동 속도는 GDD의 난이도 곡선에 맞게 계산한다.
7. 절단된 영역은 떨어지는 조각 목록에 등록한다. 조각은 중력으로 바닥까지 낙하하고, 바닥에 닿으면 한 번 튀어 오른 뒤 서서히 사라진다.

## 7. 프로토타입 완료 기준

* GDD의 블록 이동, 정지 및 절단, 퍼펙트 및 콤보, 게임오버 규칙이 모두 동작한다.
* 일반 적층 뒤 다음 이동 블록은 줄어든 직전 블록의 너비를 이어받고, 퍼펙트 뒤에는 두 단계 아래 블록에서 복구한 너비를 이어받는다.
* GDD에 정의된 모든 입력 방식에서 같은 규칙으로 플레이할 수 있다.
* 창 크기나 전체화면 전환 시에도 게임 규칙, 블록과 글자의 크기 비율이 변하지 않으며 블록이 게임 화면 밖으로 벗어나지 않는다.
* 새 게임에서 현재 층수는 0으로 초기화되며, 역대 최고 층수는 유지된다. 브라우저를 다시 열어도 저장된 최고 층수를 불러온다.
