# 스카이 스택 프로토타입 구현계획서

## 1. 프로토타입 목표

플레이어가 움직이는 블록을 멈춰 탑을 쌓고, 블록이 전혀 겹치지 않으면 게임오버가 되는 핵심 재미를 검증한다. 이번 단계에서는 연출, 효과음, 시작·결과 UI, 최고 기록 저장 기능을 구현하지 않는다.

## 2. 구현 범위

* `index.html` 한 파일에 HTML, CSS, 순수 JavaScript를 모두 작성한다.
* HTML5 Canvas 2D로 배경과 블록을 직접 그린다.
* 키보드 스페이스바, 마우스 좌클릭, 모바일 터치를 입력으로 지원한다.
* 브라우저 창 크기가 바뀌면 캔버스 크기와 게임 좌표를 다시 계산한다.

## 3. 게임 상태

| 상태 | 역할 | 입력 결과 |
| --- | --- | --- |
| `ready` | 초기 바닥 블록과 첫 이동 블록을 준비한다. | 게임을 시작하고 블록 이동을 시작한다. |
| `playing` | 이동 블록을 매 프레임 갱신한다. | 블록을 멈추고 적층 결과를 판정한다. |
| `gameover` | 블록이 전혀 겹치지 않아 게임을 멈춘 상태다. | 새 게임을 초기화하고 즉시 블록 이동을 다시 시작한다. |

첫 입력은 게임 시작에만 사용한다. `playing` 상태가 된 뒤의 입력부터 블록을 멈춘다.

## 4. 데이터 구조

* 블록은 가로 위치 `x`, 세로 위치 `y`, 가로폭 `width`, 높이 `height`, 이동 속도 `speed`, 이동 방향 `direction`을 가진다.
* `stack` 배열에는 바닥 블록과 성공적으로 적층된 블록만 저장한다.
* `movingBlock`은 현재 좌우로 움직이는 블록 한 개다.
* `score`는 바닥을 제외하고 성공적으로 쌓은 블록 수다.
* `combo`는 연속 퍼펙트 횟수다.

## 5. 업데이트와 렌더링 흐름

1. `requestAnimationFrame` 반복문에서 이전 프레임 이후 시간을 계산한다.
2. `playing` 상태이면 `movingBlock`의 `x` 좌표를 방향과 속도에 따라 이동한다.
3. 이동 블록이 화면 좌우 경계에 닿으면 위치를 경계 안으로 보정하고 방향을 반전한다.
4. 매 프레임 배경, 적층 블록, 이동 블록 순서로 캔버스에 그린다.
5. 화면 크기가 변경되면 기준 논리 크기를 유지한 채 캔버스 표시 크기와 좌표 비율을 다시 계산한다.

## 6. 적층 판정 흐름

1. 플레이어 입력 시 `movingBlock`과 `stack`의 마지막 블록의 가로 겹침 구간을 계산한다.
2. 겹치는 폭이 0 이하이면 `gameover` 상태로 전환한다.
3. 퍼펙트 허용 오차 안에서 두 블록 중심이 일치하면, 이동 블록 중심을 아래 블록 중심에 맞춘다. 블록 폭은 최초 최대 폭으로 복구하고 `combo`를 1 증가시킨다.
4. 퍼펙트가 아니면 겹치는 구간의 위치와 폭만 남긴다. `combo`는 0으로 초기화한다.
5. 확정된 블록을 `stack`에 추가하고 점수를 1 증가시킨다.
6. 새 이동 블록을 방금 확정한 블록 바로 위에 생성한다. 새 블록의 폭은 확정 블록의 폭을 사용한다.
7. 점수에 따라 이동 속도를 올리되, 최대 속도를 넘지 않게 제한한다.

## 7. 프로토타입 완료 기준

* 첫 입력 이후 블록이 좌우로 계속 왕복한다.
* 입력 시 겹친 부분만 남아 다음 층이 된다.
* 퍼펙트 시 정렬과 최대 폭 복구가 동작한다.
* 겹침이 없으면 더 이상 새 블록이 생성되지 않는다.
* PC와 모바일 입력 모두에서 같은 규칙으로 플레이할 수 있다.
* 창 크기를 바꿔도 블록이 캔버스 밖으로 벗어나지 않는다.
